sudo rngd -r /dev/urandom
gpg --gen-key

aptly publish repo release s3:spinnaker-debian-repo: -skip-signing
docker build -t builddep -f Dockerfile.builddep .
sudo docker exec builddep bash -c 'cd /root; apt-get install -y npm ;git-buildpackage ;cp ../*.deb . ;exit'

vi ~/.aptly.conf

  "S3PublishEndpoints": {
    "spinnaker-debian-repo": {
      "region": "us-east-1",
      "bucket": "spinnaker-debian-repo-jorn",
      "acl": "public-read",
      "awsAccessKeyID": "access key id",
      "awsSecretAccessKey": "aws secret access key"
    }
  },